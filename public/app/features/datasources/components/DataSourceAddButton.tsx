import { type JSX } from 'react';

// Curated instance: data sources are provisioned, so the "Add new data source"
// button is hidden. Returns null unconditionally.
export function DataSourceAddButton(): JSX.Element | null {
  return null;
}
